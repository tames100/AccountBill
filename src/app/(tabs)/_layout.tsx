
import React from 'react';
import {View} from 'react-native';

interface _layoutProps {
}

/**
 * 组件介绍
 * @param props
 * @constructor
 */
export default function _layout(props: _layoutProps) {
  const {} = props;
  const title = "123";

  return (
    <View>{title}</View>
  );
}