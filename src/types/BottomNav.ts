
import React from 'react';
import {View} from 'react-native';

interface ButtomNavProps {
}

/**
 * 组件介绍
 * @param props
 * @constructor
 */
export default function ButtomNav(props: ButtomNavProps) {
  const {} = props;
  const title = "123";

  return (
    <View>{title}</View>
  );
}